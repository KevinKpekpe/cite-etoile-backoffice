<x-layouts.guest title="Vérification du reçu"><div class="text-center"><p class="text-sm text-slate-500">Reçu</p><h2 class="text-2xl font-bold">{{ $receipt->receipt_number }}</h2><p class="my-5 text-3xl font-bold">{{ $receipt->amount }} USD</p><p class="rounded-lg p-3 {{ $receipt->status === 'valid' ? 'bg-emerald-100 text-emerald-900' : 'bg-red-100 text-red-900' }}">{{ $receipt->status === 'valid' ? 'Ce reçu est valide.' : 'Ce reçu a été annulé.' }}</p><p class="mt-5 text-sm text-slate-500">Émis le {{ $receipt->issued_at->format('d/m/Y H:i') }}</p></div></x-layouts.guest>
