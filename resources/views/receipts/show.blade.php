<x-layouts.app :title="$receipt->receipt_number"><div class="mx-auto max-w-3xl rounded-xl bg-white p-8 shadow-sm"><div class="border-b pb-5 text-center"><p class="text-sm font-bold text-amber-700">MJIC IMMOBILIER SARL</p><h1 class="text-3xl font-bold">Reçu {{ $receipt->receipt_number }}</h1><p>{{ $receipt->status === 'valid' ? 'Valide' : 'Annulé' }}</p></div><p class="my-8 text-center text-4xl font-bold">{{ $receipt->amount }} USD</p><dl class="grid gap-3 text-sm md:grid-cols-2"><div><dt class="text-slate-500">Client</dt><dd>{{ $receipt->customer->first_name }} {{ $receipt->customer->last_name }}</dd></div><div><dt class="text-slate-500">Parcelle</dt><dd>{{ $receipt->subscription->plot->reference }}</dd></div><div><dt class="text-slate-500">Paiement</dt><dd>{{ $receipt->payment->payment_reference }}</dd></div><div><dt class="text-slate-500">Date</dt><dd>{{ $receipt->issued_at->format('d/m/Y H:i') }}</dd></div></dl>@can('receipts.download')<a href="{{ route('receipts.download',$receipt) }}" class="mt-8 inline-block rounded-lg bg-slate-950 px-5 py-3 text-white">Télécharger le PDF</a>@endcan</div></x-layouts.app>
