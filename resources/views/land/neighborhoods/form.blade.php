<x-layouts.app title="Quartier"><form method="POST" action="{{ $neighborhood->exists ? route('neighborhoods.update',$neighborhood) : route('neighborhoods.store') }}" class="mx-auto flex max-w-2xl flex-col gap-5 rounded-xl bg-white p-6 shadow-sm">@csrf @if($neighborhood->exists)@method('PUT')@endif<h1 class="text-2xl font-bold">{{ $neighborhood->exists ? 'Modifier le quartier' : 'Nouveau quartier' }}</h1><x-auth-input name="code" label="Code" :value="old('code',$neighborhood->code)" required/><x-auth-input name="name" label="Nom" :value="old('name',$neighborhood->name)" required/><label class="flex flex-col gap-2">Statut<select name="status" class="rounded-lg border p-3">@foreach(['planned','active','commercializable','completed','suspended'] as $status)<option @selected(old('status',$neighborhood->status)===$status)>{{ $status }}</option>@endforeach</select></label><label class="flex flex-col gap-2">Description<textarea name="description" class="rounded-lg border p-3">{{ old('description',$neighborhood->description) }}</textarea></label><button class="rounded-lg bg-slate-950 p-3 text-white">Enregistrer</button></form></x-layouts.app>
