<x-layouts.app title="Avenue"><form method="POST" action="{{ $avenue->exists ? route('avenues.update',$avenue) : route('avenues.store') }}" class="mx-auto flex max-w-2xl flex-col gap-5 rounded-xl bg-white p-6 shadow-sm">@csrf @if($avenue->exists)@method('PUT')@endif<h1 class="text-2xl font-bold">{{ $avenue->exists ? 'Modifier l’avenue' : 'Nouvelle avenue' }}</h1><label>Quartier<select name="neighborhood_id" class="mt-2 w-full rounded-lg border p-3">@foreach($neighborhoods as $item)<option value="{{ $item->id }}" @selected(old('neighborhood_id',$avenue->neighborhood_id)===$item->id)>{{ $item->name }}</option>@endforeach</select></label><x-auth-input name="code" label="Code" :value="old('code',$avenue->code)" required/><x-auth-input name="name" label="Nom" :value="old('name',$avenue->name)" required/><label>Statut<select name="status" class="mt-2 w-full rounded-lg border p-3">@foreach(['planned','active','suspended'] as $status)<option @selected(old('status',$avenue->status)===$status)>{{ $status }}</option>@endforeach</select></label><textarea name="description" class="rounded-lg border p-3" placeholder="Description">{{ old('description',$avenue->description) }}</textarea><button class="rounded-lg bg-slate-950 p-3 text-white">Enregistrer</button></form></x-layouts.app>
