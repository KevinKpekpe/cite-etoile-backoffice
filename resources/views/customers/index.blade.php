<x-layouts.app title="Clients"><div class="mb-6 flex flex-wrap items-center justify-between gap-4"><div><h1 class="text-3xl font-bold">Clients</h1><p class="text-slate-600">Recherche et suivi des dossiers clients.</p></div>@can('customers.create')<a href="{{ route('customers.create') }}" class="rounded-lg bg-amber-600 px-4 py-2 font-semibold text-white">Nouveau client</a>@endcan</div>
<form class="mb-6 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-[1fr_220px_auto]"><input name="search" value="{{ $filters['search'] ?? '' }}" placeholder="Nom, téléphone ou numéro client" class="rounded-lg border px-3 py-2"><select name="status" class="rounded-lg border px-3 py-2"><option value="">Tous les statuts</option>@foreach(['prospect','active','settled','suspended','archived'] as $status)<option @selected(($filters['status']??'')===$status) value="{{ $status }}">{{ ucfirst($status) }}</option>@endforeach</select><button class="rounded-lg bg-slate-950 px-4 py-2 text-white">Rechercher</button></form>
<div class="overflow-hidden rounded-xl bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-100"><tr><th class="p-4">N° client</th><th class="p-4">Identité</th><th class="p-4">Téléphone</th><th class="p-4">Statut</th><th class="p-4">Responsable</th></tr></thead><tbody class="divide-y">@forelse($customers as $customer)<tr><td class="p-4 font-mono"><a class="text-amber-700" href="{{ route('customers.show',$customer) }}">{{ $customer->customer_number }}</a></td><td class="p-4 font-medium">{{ $customer->first_name }} {{ $customer->last_name }}</td><td class="p-4">{{ $customer->phone }}</td><td class="p-4">{{ $customer->status }}</td><td class="p-4">{{ $customer->assignedAgent ? $customer->assignedAgent->first_name.' '.$customer->assignedAgent->last_name : '—' }}</td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-slate-500">Aucun client trouvé.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $customers->links() }}</div></x-layouts.app>
