<x-layouts.app title="Mes reçus"><div class="flex flex-col gap-5"><h1 class="text-3xl font-bold">Mes reçus</h1><div class="overflow-hidden rounded-xl bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-4">Date</th><th class="p-4">Reçu</th><th class="p-4">Parcelle</th><th class="p-4">Montant</th><th class="p-4"></th></tr></thead><tbody>@forelse($receipts as $receipt)<tr class="border-t"><td class="p-4">{{ $receipt->issued_at->format('d/m/Y') }}</td><td class="p-4">{{ $receipt->receipt_number }}</td><td class="p-4">{{ $receipt->subscription->plot->reference }}</td><td class="p-4">{{ number_format((float) $receipt->amount, 2, ',', ' ') }} USD</td><td class="p-4">@if($receipt->status === 'valid')<a class="font-semibold text-amber-700" href="{{ URL::temporarySignedRoute('portal.receipts.download', now()->addMinutes(10), $receipt) }}">Télécharger</a>@else<span class="text-red-700">Annulé</span>@endif</td></tr>@empty<tr><td colspan="5" class="p-6 text-center">Aucun reçu.</td></tr>@endforelse</tbody></table></div>{{ $receipts->links() }}</div></x-layouts.app>
