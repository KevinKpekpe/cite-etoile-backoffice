<x-layouts.app :title="$subscription->subscription_number"><div class="flex flex-col gap-6"><div><a class="text-sm text-amber-700" href="{{ route('portal.subscriptions.index') }}">← Mes souscriptions</a><h1 class="text-3xl font-bold">{{ $subscription->subscription_number }}</h1></div><dl class="grid gap-4 rounded-xl bg-white p-6 shadow-sm md:grid-cols-3"><div><dt class="text-sm text-slate-500">Parcelle</dt><dd class="font-semibold">{{ $subscription->plot->reference }}</dd></div><div><dt class="text-sm text-slate-500">Localisation</dt><dd>{{ $subscription->plot->avenue->name }}, {{ $subscription->plot->avenue->neighborhood->name }}</dd></div><div><dt class="text-sm text-slate-500">Formule</dt><dd>{{ $subscription->paymentPlan->name }}</dd></div><div><dt class="text-sm text-slate-500">Total</dt><dd>{{ number_format((float) $subscription->contract_total, 2, ',', ' ') }} USD</dd></div><div><dt class="text-sm text-slate-500">Payé</dt><dd>{{ number_format((float) $subscription->amount_paid, 2, ',', ' ') }} USD</dd></div><div><dt class="text-sm text-slate-500">Reste</dt><dd>{{ number_format((float) $subscription->balance, 2, ',', ' ') }} USD</dd></div></dl><div class="rounded-xl bg-white p-6 shadow-sm"><h2 class="mb-4 text-xl font-bold">Échéancier</h2><div class="flex flex-col gap-2">@forelse($subscription->installments as $installment)<div class="flex justify-between rounded-lg bg-slate-50 p-3"><span>N° {{ $installment->installment_number }} · {{ $installment->due_date->format('d/m/Y') }}</span><span>{{ $installment->status }} · {{ number_format((float) $installment->balance, 2, ',', ' ') }} USD</span></div>@empty<p>Aucune échéance.</p>@endforelse</div></div></div></x-layouts.app>
