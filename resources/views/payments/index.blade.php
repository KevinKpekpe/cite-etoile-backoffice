<x-layouts.app title="Paiements"><h1 class="text-3xl font-bold">Encaissements</h1><p class="mb-6 text-slate-600">Recherchez un dossier avant d’enregistrer un paiement.</p><form class="mb-5 flex gap-3 rounded-xl bg-white p-4 shadow-sm"><input name="search" value="{{ $search }}" placeholder="Client, téléphone, contrat, parcelle…" class="grow rounded-lg border p-3"><button class="rounded-lg bg-slate-950 px-5 text-white">Rechercher</button></form><div class="overflow-x-auto rounded-xl bg-white p-5 shadow-sm"><table class="w-full text-left text-sm"><thead><tr><th class="py-3">Souscription</th><th>Client</th><th>Parcelle</th><th>Contractuel</th><th>Payé</th><th>Solde</th><th></th></tr></thead><tbody class="divide-y">@foreach($subscriptions as $subscription)<tr><td class="py-3 font-mono">{{ $subscription->subscription_number }}</td><td>{{ $subscription->customer->first_name }} {{ $subscription->customer->last_name }}</td><td>{{ $subscription->plot->reference }}</td><td>{{ $subscription->contract_total }}</td><td>{{ $subscription->amount_paid }}</td><td>{{ $subscription->balance }}</td><td>@can('payments.create')<a href="{{ route('payments.create',$subscription) }}" class="text-amber-700">Encaisser</a>@endcan</td></tr>@endforeach</tbody></table></div><div class="mt-5">{{ $subscriptions->links() }}</div></x-layouts.app>
