<x-layouts.app title="Nouveau paiement"><div class="mx-auto max-w-3xl"><div class="mb-5"><h1 class="text-3xl font-bold">Nouveau paiement</h1><p>{{ $subscription->customer->first_name }} {{ $subscription->customer->last_name }} · {{ $subscription->plot->reference }}</p></div><div class="mb-5 grid grid-cols-3 gap-3">@foreach([['Contractuel',$subscription->contract_total],['Payé',$subscription->amount_paid],['Solde',$subscription->balance]] as [$label,$amount])<div class="rounded-xl bg-white p-4 shadow-sm"><p class="text-xs text-slate-500">{{ $label }}</p><strong>{{ $amount }} USD</strong></div>@endforeach</div><form method="POST" enctype="multipart/form-data" action="{{ route('payments.store') }}" class="grid gap-5 rounded-xl bg-white p-6 shadow-sm md:grid-cols-2">@csrf<input type="hidden" name="subscription_id" value="{{ $subscription->id }}"><input type="hidden" name="idempotency_key" value="{{ $idempotencyKey }}"><x-auth-input name="payment_date" label="Date du paiement" type="datetime-local" :value="old('payment_date',now()->format('Y-m-d\TH:i'))" required/><x-auth-input name="amount" label="Montant USD" type="number" step="0.01" :value="old('amount')" required/><input type="hidden" name="currency" value="USD"><label>Mode<select name="payment_method" class="mt-2 w-full rounded-lg border p-3">@foreach(['cash'=>'Espèces','bank_transfer'=>'Virement','mobile_money'=>'Mobile money','card'=>'Carte','other'=>'Autre'] as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></label><x-auth-input name="transaction_reference" label="Référence externe" :value="old('transaction_reference')"/><label class="md:col-span-2">Preuve<input type="file" name="proof" accept=".pdf,.jpg,.jpeg,.png,.webp" class="mt-2 block"></label><textarea name="notes" class="rounded-lg border p-3 md:col-span-2" placeholder="Commentaire">{{ old('notes') }}</textarea><button class="rounded-lg bg-slate-950 p-3 text-white md:col-span-2">Valider le paiement</button></form></div></x-layouts.app>
